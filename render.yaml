services:
  - type: web
    name: matchmovies
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn "server.app:create_app()"
    envVars:
      - key: DATABASE_URL
        value: postgresql+psycopg://postgres:SUA_NOVA_SENHA_AQUI@db.kwuxogvfhfgryiquhshl.supabase.co:5432/postgres?sslmode=require
      - key: FLASK_ENV
        value: production
      - key: SECRET_KEY
        generateValue: true
